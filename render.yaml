services:
  - type: web
    name: teen-patti-royale
    env: node
    plan: free
    branch: Master
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 20.18.0
      - key: NODE_ENV
        value: production
